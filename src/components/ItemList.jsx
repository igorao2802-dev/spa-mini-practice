// TimeSlot.jsx
// ПОЧЕМУ? Компонент оставлен минимальным на данном этапе практики,
// так как основная цель — отработать props, state и декомпозицию.
// В будущих этапах здесь будет реализован выбор временных слотов.
import React from "react";

// ПОЧЕМУ? Компонент назван TimeSlot в соответствии с темой курсового проекта
// («Система онлайн-бронирования помещений»), где выбор времени — ключевая функция.
const TimeSlot = (props) => {
  return (
    // ПОЧЕМУ? Пустой div с классом — подготовка к будущим стилям и логике.
    // Это позволяет постепенно наращивать функционал без переписывания структуры.
    <div className="time-slot-placeholder">
      {/* Здесь будет отображение доступных временных слотов */}
    </div>
  );
};

export default TimeSlot;